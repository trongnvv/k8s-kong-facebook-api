apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: service-auth
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
    - http:
        paths:
          - path: /api
            backend:
              serviceName: service-auth
              servicePort: 8031
